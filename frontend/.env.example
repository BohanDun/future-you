# Copy to .env.local for local development.
# Use the API Gateway base URL when testing the deployed backend.
VITE_API_URL=http://127.0.0.1:8000
VITE_AUTH_MODE=mock
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
